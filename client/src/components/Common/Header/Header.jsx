import styles from './Header.module.css'

const Header = () => {
  return (
    <header className={styles.header}>
      <p className={styles.silence}>Silence</p>
    </header>
  )
}

export default Header
